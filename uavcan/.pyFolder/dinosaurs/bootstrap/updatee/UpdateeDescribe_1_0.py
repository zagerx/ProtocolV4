# AUTOGENERATED, DO NOT EDIT.
#
# Source file:
# /home/zhangge/worknote/ProtocolV4/uavcan/custom_data_types/dinosaurs/bootstrap/updatee/UpdateeDescribe.1.0.uavcan
#
# Generated at:  2025-06-25 01:48:15.085608 UTC
# Is deprecated: no
# Fixed port ID: None
# Full name:     dinosaurs.bootstrap.updatee.UpdateeDescribe
# Version:       1.0
#
# pylint: skip-file
# mypy: warn_unused_ignores=False

from __future__ import annotations
from nunavut_support import Serializer as _Serializer_, Deserializer as _Deserializer_, API_VERSION as _NSAPIV_
import numpy as _np_
from numpy.typing import NDArray as _NDArray_
import pydsdl as _pydsdl_
import dinosaurs.bootstrap.updatee

if _NSAPIV_[0] != 1:
    raise RuntimeError(
        f"Incompatible Nunavut support API version: support { _NSAPIV_ }, package (1, 0, 0)"
    )

def _restore_constant_(encoded_string: str) -> object:
    import pickle, gzip, base64
    return pickle.loads(gzip.decompress(base64.b85decode(encoded_string)))

# noinspection PyUnresolvedReferences, PyPep8, PyPep8Naming, SpellCheckingInspection, DuplicatedCode
class UpdateeDescribe_1_0:
    """
    Generated property settings use relaxed type signatures, accepting a large variety of
    possible representations of the value, which are automatically converted to a well-defined
    internal representation. When accessing a property, this strict well-defined internal
    representation is always returned. The implicit strictification enables more precise static
    type analysis.

    The value returned by the __repr__() method may be invariant to some of the field values,
    and its format is not guaranteed to be stable. Therefore, the returned string representation
    can be used only for displaying purposes; any kind of automation build on top of that will
    be fragile and prone to mismaintenance.
    """
    def __init__(self,
                 name:             None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str = None,
                 file_name:        None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str = None,
                 file_path:        None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str = None,
                 build_number:     None | int | _np_.uint64 = None,
                 build_time:       None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str = None,
                 software_version: None | dinosaurs.bootstrap.updatee.Version_1_0 = None,
                 unique_id:        None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray  = None,
                 hardware_version: None | dinosaurs.bootstrap.updatee.Version_1_0 = None) -> None:
        """
        dinosaurs.bootstrap.updatee.UpdateeDescribe.1.0
        Raises ValueError if any of the primitive values are outside the permitted range, regardless of the cast mode.
        :param name:             saturated uint8[<=50] name
        :param file_name:        saturated uint8[<=50] file_name
        :param file_path:        saturated uint8[<=150] file_path
        :param build_number:     saturated uint64 build_number
        :param build_time:       saturated uint8[<=30] build_time
        :param software_version: dinosaurs.bootstrap.updatee.Version.1.0 software_version
        :param unique_id:        saturated uint8[16] unique_id
        :param hardware_version: dinosaurs.bootstrap.updatee.Version.1.0 hardware_version
        """
        self._name:             _NDArray_[_np_.uint8]
        self._file_name:        _NDArray_[_np_.uint8]
        self._file_path:        _NDArray_[_np_.uint8]
        self._build_number:     int
        self._build_time:       _NDArray_[_np_.uint8]
        self._software_version: dinosaurs.bootstrap.updatee.Version_1_0
        self._unique_id:        _NDArray_[_np_.uint8]
        self._hardware_version: dinosaurs.bootstrap.updatee.Version_1_0

        if name is None:
            self.name = _np_.array([], _np_.uint8)
        else:
            name = name.encode() if isinstance(name, str) else name  # Implicit string encoding
            if isinstance(name, (bytes, bytearray)) and len(name) <= 50:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._name = _np_.frombuffer(name, _np_.uint8)  # type: ignore
            elif isinstance(name, _np_.ndarray) and name.dtype == _np_.uint8 and name.ndim == 1 and name.size <= 50:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._name = name
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                name = _np_.array(name, _np_.uint8).flatten()
                if not name.size <= 50:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'name: invalid array length: not {name.size} <= 50')
                self._name = name
            assert isinstance(self._name, _np_.ndarray)
            assert self._name.dtype == _np_.uint8  # type: ignore
            assert self._name.ndim == 1
            assert len(self._name) <= 50

        if file_name is None:
            self.file_name = _np_.array([], _np_.uint8)
        else:
            file_name = file_name.encode() if isinstance(file_name, str) else file_name  # Implicit string encoding
            if isinstance(file_name, (bytes, bytearray)) and len(file_name) <= 50:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._file_name = _np_.frombuffer(file_name, _np_.uint8)  # type: ignore
            elif isinstance(file_name, _np_.ndarray) and file_name.dtype == _np_.uint8 and file_name.ndim == 1 and file_name.size <= 50:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._file_name = file_name
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                file_name = _np_.array(file_name, _np_.uint8).flatten()
                if not file_name.size <= 50:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'file_name: invalid array length: not {file_name.size} <= 50')
                self._file_name = file_name
            assert isinstance(self._file_name, _np_.ndarray)
            assert self._file_name.dtype == _np_.uint8  # type: ignore
            assert self._file_name.ndim == 1
            assert len(self._file_name) <= 50

        if file_path is None:
            self.file_path = _np_.array([], _np_.uint8)
        else:
            file_path = file_path.encode() if isinstance(file_path, str) else file_path  # Implicit string encoding
            if isinstance(file_path, (bytes, bytearray)) and len(file_path) <= 150:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._file_path = _np_.frombuffer(file_path, _np_.uint8)  # type: ignore
            elif isinstance(file_path, _np_.ndarray) and file_path.dtype == _np_.uint8 and file_path.ndim == 1 and file_path.size <= 150:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._file_path = file_path
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                file_path = _np_.array(file_path, _np_.uint8).flatten()
                if not file_path.size <= 150:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'file_path: invalid array length: not {file_path.size} <= 150')
                self._file_path = file_path
            assert isinstance(self._file_path, _np_.ndarray)
            assert self._file_path.dtype == _np_.uint8  # type: ignore
            assert self._file_path.ndim == 1
            assert len(self._file_path) <= 150

        self.build_number = build_number if build_number is not None else 0  # type: ignore

        if build_time is None:
            self.build_time = _np_.array([], _np_.uint8)
        else:
            build_time = build_time.encode() if isinstance(build_time, str) else build_time  # Implicit string encoding
            if isinstance(build_time, (bytes, bytearray)) and len(build_time) <= 30:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._build_time = _np_.frombuffer(build_time, _np_.uint8)  # type: ignore
            elif isinstance(build_time, _np_.ndarray) and build_time.dtype == _np_.uint8 and build_time.ndim == 1 and build_time.size <= 30:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._build_time = build_time
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                build_time = _np_.array(build_time, _np_.uint8).flatten()
                if not build_time.size <= 30:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'build_time: invalid array length: not {build_time.size} <= 30')
                self._build_time = build_time
            assert isinstance(self._build_time, _np_.ndarray)
            assert self._build_time.dtype == _np_.uint8  # type: ignore
            assert self._build_time.ndim == 1
            assert len(self._build_time) <= 30

        if software_version is None:
            self.software_version = dinosaurs.bootstrap.updatee.Version_1_0()
        elif isinstance(software_version, dinosaurs.bootstrap.updatee.Version_1_0):
            self.software_version = software_version
        else:
            raise ValueError(f'software_version: expected dinosaurs.bootstrap.updatee.Version_1_0 '
                             f'got {type(software_version).__name__}')

        if unique_id is None:
            self.unique_id = _np_.zeros(16, _np_.uint8)
        else:
            if isinstance(unique_id, (bytes, bytearray)) and len(unique_id) == 16:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._unique_id = _np_.frombuffer(unique_id, _np_.uint8)  # type: ignore
            elif isinstance(unique_id, _np_.ndarray) and unique_id.dtype == _np_.uint8 and unique_id.ndim == 1 and unique_id.size == 16:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._unique_id = unique_id
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                unique_id = _np_.array(unique_id, _np_.uint8).flatten()
                if not unique_id.size == 16:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'unique_id: invalid array length: not {unique_id.size} == 16')
                self._unique_id = unique_id
            assert isinstance(self._unique_id, _np_.ndarray)
            assert self._unique_id.dtype == _np_.uint8  # type: ignore
            assert self._unique_id.ndim == 1
            assert len(self._unique_id) == 16

        if hardware_version is None:
            self.hardware_version = dinosaurs.bootstrap.updatee.Version_1_0()
        elif isinstance(hardware_version, dinosaurs.bootstrap.updatee.Version_1_0):
            self.hardware_version = hardware_version
        else:
            raise ValueError(f'hardware_version: expected dinosaurs.bootstrap.updatee.Version_1_0 '
                             f'got {type(hardware_version).__name__}')

    @property
    def name(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[<=50] name
        DSDL does not support strings natively yet. To interpret this array as a string,
        use tobytes() to convert the NumPy array to bytes, and then decode() to convert bytes to string:
        .name.tobytes().decode()
        When assigning a string to this property, no manual conversion is necessary (it will happen automatically).
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._name

    @name.setter
    def name(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str) -> None:
        x = x.encode() if isinstance(x, str) else x  # Implicit string encoding
        if isinstance(x, (bytes, bytearray)) and len(x) <= 50:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._name = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size <= 50:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._name = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size <= 50:  # Length cannot be checked before casting and flattening
                raise ValueError(f'name: invalid array length: not {x.size} <= 50')
            self._name = x
        assert isinstance(self._name, _np_.ndarray)
        assert self._name.dtype == _np_.uint8  # type: ignore
        assert self._name.ndim == 1
        assert len(self._name) <= 50

    @property
    def file_name(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[<=50] file_name
        DSDL does not support strings natively yet. To interpret this array as a string,
        use tobytes() to convert the NumPy array to bytes, and then decode() to convert bytes to string:
        .file_name.tobytes().decode()
        When assigning a string to this property, no manual conversion is necessary (it will happen automatically).
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._file_name

    @file_name.setter
    def file_name(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str) -> None:
        x = x.encode() if isinstance(x, str) else x  # Implicit string encoding
        if isinstance(x, (bytes, bytearray)) and len(x) <= 50:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._file_name = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size <= 50:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._file_name = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size <= 50:  # Length cannot be checked before casting and flattening
                raise ValueError(f'file_name: invalid array length: not {x.size} <= 50')
            self._file_name = x
        assert isinstance(self._file_name, _np_.ndarray)
        assert self._file_name.dtype == _np_.uint8  # type: ignore
        assert self._file_name.ndim == 1
        assert len(self._file_name) <= 50

    @property
    def file_path(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[<=150] file_path
        DSDL does not support strings natively yet. To interpret this array as a string,
        use tobytes() to convert the NumPy array to bytes, and then decode() to convert bytes to string:
        .file_path.tobytes().decode()
        When assigning a string to this property, no manual conversion is necessary (it will happen automatically).
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._file_path

    @file_path.setter
    def file_path(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str) -> None:
        x = x.encode() if isinstance(x, str) else x  # Implicit string encoding
        if isinstance(x, (bytes, bytearray)) and len(x) <= 150:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._file_path = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size <= 150:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._file_path = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size <= 150:  # Length cannot be checked before casting and flattening
                raise ValueError(f'file_path: invalid array length: not {x.size} <= 150')
            self._file_path = x
        assert isinstance(self._file_path, _np_.ndarray)
        assert self._file_path.dtype == _np_.uint8  # type: ignore
        assert self._file_path.ndim == 1
        assert len(self._file_path) <= 150

    @property
    def build_number(self) -> int:
        """
        saturated uint64 build_number
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._build_number

    @build_number.setter
    def build_number(self, x: int | _np_.uint64) -> None:
        """Raises ValueError if the value is outside of the permitted range, regardless of the cast mode."""
        x = int(x)
        if 0 <= x <= 18446744073709551615:
            self._build_number = x
        else:
            raise ValueError(f'build_number: value {x} is not in [0, 18446744073709551615]')

    @property
    def build_time(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[<=30] build_time
        DSDL does not support strings natively yet. To interpret this array as a string,
        use tobytes() to convert the NumPy array to bytes, and then decode() to convert bytes to string:
        .build_time.tobytes().decode()
        When assigning a string to this property, no manual conversion is necessary (it will happen automatically).
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._build_time

    @build_time.setter
    def build_time(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str) -> None:
        x = x.encode() if isinstance(x, str) else x  # Implicit string encoding
        if isinstance(x, (bytes, bytearray)) and len(x) <= 30:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._build_time = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size <= 30:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._build_time = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size <= 30:  # Length cannot be checked before casting and flattening
                raise ValueError(f'build_time: invalid array length: not {x.size} <= 30')
            self._build_time = x
        assert isinstance(self._build_time, _np_.ndarray)
        assert self._build_time.dtype == _np_.uint8  # type: ignore
        assert self._build_time.ndim == 1
        assert len(self._build_time) <= 30

    @property
    def software_version(self) -> dinosaurs.bootstrap.updatee.Version_1_0:
        """
        dinosaurs.bootstrap.updatee.Version.1.0 software_version
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._software_version

    @software_version.setter
    def software_version(self, x: dinosaurs.bootstrap.updatee.Version_1_0) -> None:
        if isinstance(x, dinosaurs.bootstrap.updatee.Version_1_0):
            self._software_version = x
        else:
            raise ValueError(f'software_version: expected dinosaurs.bootstrap.updatee.Version_1_0 got {type(x).__name__}')

    @property
    def unique_id(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[16] unique_id
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._unique_id

    @unique_id.setter
    def unique_id(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray ) -> None:
        if isinstance(x, (bytes, bytearray)) and len(x) == 16:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._unique_id = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size == 16:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._unique_id = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size == 16:  # Length cannot be checked before casting and flattening
                raise ValueError(f'unique_id: invalid array length: not {x.size} == 16')
            self._unique_id = x
        assert isinstance(self._unique_id, _np_.ndarray)
        assert self._unique_id.dtype == _np_.uint8  # type: ignore
        assert self._unique_id.ndim == 1
        assert len(self._unique_id) == 16

    @property
    def hardware_version(self) -> dinosaurs.bootstrap.updatee.Version_1_0:
        """
        dinosaurs.bootstrap.updatee.Version.1.0 hardware_version
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._hardware_version

    @hardware_version.setter
    def hardware_version(self, x: dinosaurs.bootstrap.updatee.Version_1_0) -> None:
        if isinstance(x, dinosaurs.bootstrap.updatee.Version_1_0):
            self._hardware_version = x
        else:
            raise ValueError(f'hardware_version: expected dinosaurs.bootstrap.updatee.Version_1_0 got {type(x).__name__}')

    # noinspection PyProtectedMember
    def _serialize_(self, _ser_: _Serializer_) -> None:
        assert _ser_.current_bit_length % 8 == 0, 'Serializer is not aligned'
        _base_offset_ = _ser_.current_bit_length
        # Variable-length array: length field byte-aligned: True; all elements byte-aligned: True.
        assert len(self.name) <= 50, 'self.name: saturated uint8[<=50]'
        _ser_.add_aligned_u8(len(self.name))
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.name)
        # Variable-length array: length field byte-aligned: True; all elements byte-aligned: True.
        assert len(self.file_name) <= 50, 'self.file_name: saturated uint8[<=50]'
        _ser_.add_aligned_u8(len(self.file_name))
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.file_name)
        # Variable-length array: length field byte-aligned: True; all elements byte-aligned: True.
        assert len(self.file_path) <= 150, 'self.file_path: saturated uint8[<=150]'
        _ser_.add_aligned_u8(len(self.file_path))
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.file_path)
        _ser_.add_aligned_u64(max(min(self.build_number, 18446744073709551615), 0))
        # Variable-length array: length field byte-aligned: True; all elements byte-aligned: True.
        assert len(self.build_time) <= 30, 'self.build_time: saturated uint8[<=30]'
        _ser_.add_aligned_u8(len(self.build_time))
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.build_time)
        _ser_.pad_to_alignment(8)
        self.software_version._serialize_(_ser_)
        assert _ser_.current_bit_length % 8 == 0, 'Nested object alignment error'
        assert len(self.unique_id) == 16, 'self.unique_id: saturated uint8[16]'
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.unique_id)
        _ser_.pad_to_alignment(8)
        self.hardware_version._serialize_(_ser_)
        assert _ser_.current_bit_length % 8 == 0, 'Nested object alignment error'
        _ser_.pad_to_alignment(8)
        assert 256 <= (_ser_.current_bit_length - _base_offset_) <= 2496, \
            'Bad serialization of dinosaurs.bootstrap.updatee.UpdateeDescribe.1.0'

    # noinspection PyProtectedMember
    @staticmethod
    def _deserialize_(_des_: _Deserializer_) -> UpdateeDescribe_1_0:
        assert _des_.consumed_bit_length % 8 == 0, 'Deserializer is not aligned'
        _base_offset_ = _des_.consumed_bit_length
        # Temporary _f0_ holds the value of "name"
        # Length field byte-aligned: True; all elements byte-aligned: True.
        _len0_ = _des_.fetch_aligned_u8()
        assert _len0_ >= 0
        if _len0_ > 50:
            raise _des_.FormatError(f'Variable array length prefix {_len0_} > 50')
        _f0_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, _len0_)
        assert len(_f0_) <= 50, 'saturated uint8[<=50]'
        # Temporary _f1_ holds the value of "file_name"
        # Length field byte-aligned: True; all elements byte-aligned: True.
        _len1_ = _des_.fetch_aligned_u8()
        assert _len1_ >= 0
        if _len1_ > 50:
            raise _des_.FormatError(f'Variable array length prefix {_len1_} > 50')
        _f1_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, _len1_)
        assert len(_f1_) <= 50, 'saturated uint8[<=50]'
        # Temporary _f2_ holds the value of "file_path"
        # Length field byte-aligned: True; all elements byte-aligned: True.
        _len2_ = _des_.fetch_aligned_u8()
        assert _len2_ >= 0
        if _len2_ > 150:
            raise _des_.FormatError(f'Variable array length prefix {_len2_} > 150')
        _f2_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, _len2_)
        assert len(_f2_) <= 150, 'saturated uint8[<=150]'
        # Temporary _f3_ holds the value of "build_number"
        _f3_ = _des_.fetch_aligned_u64()
        # Temporary _f4_ holds the value of "build_time"
        # Length field byte-aligned: True; all elements byte-aligned: True.
        _len3_ = _des_.fetch_aligned_u8()
        assert _len3_ >= 0
        if _len3_ > 30:
            raise _des_.FormatError(f'Variable array length prefix {_len3_} > 30')
        _f4_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, _len3_)
        assert len(_f4_) <= 30, 'saturated uint8[<=30]'
        # Temporary _f5_ holds the value of "software_version"
        _des_.pad_to_alignment(8)
        _f5_ = dinosaurs.bootstrap.updatee.Version_1_0._deserialize_(_des_)
        assert _des_.consumed_bit_length % 8 == 0, 'Nested object alignment error'
        # Temporary _f6_ holds the value of "unique_id"
        _f6_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, 16)
        assert len(_f6_) == 16, 'saturated uint8[16]'
        # Temporary _f7_ holds the value of "hardware_version"
        _des_.pad_to_alignment(8)
        _f7_ = dinosaurs.bootstrap.updatee.Version_1_0._deserialize_(_des_)
        assert _des_.consumed_bit_length % 8 == 0, 'Nested object alignment error'
        self = UpdateeDescribe_1_0(
            name=_f0_,
            file_name=_f1_,
            file_path=_f2_,
            build_number=_f3_,
            build_time=_f4_,
            software_version=_f5_,
            unique_id=_f6_,
            hardware_version=_f7_)
        _des_.pad_to_alignment(8)
        assert 256 <= (_des_.consumed_bit_length - _base_offset_) <= 2496, \
            'Bad deserialization of dinosaurs.bootstrap.updatee.UpdateeDescribe.1.0'
        assert isinstance(self, UpdateeDescribe_1_0)
        return self

    def __repr__(self) -> str:
        _o_0_ = ', '.join([
            'name=%s' % repr(bytes(self.name))[1:],
            'file_name=%s' % repr(bytes(self.file_name))[1:],
            'file_path=%s' % repr(bytes(self.file_path))[1:],
            'build_number=%s' % self.build_number,
            'build_time=%s' % repr(bytes(self.build_time))[1:],
            'software_version=%s' % self.software_version,
            'unique_id=%s' % _np_.array2string(self.unique_id, separator=',', edgeitems=10, threshold=100, max_line_width=1000000),
            'hardware_version=%s' % self.hardware_version,
        ])
        return f'dinosaurs.bootstrap.updatee.UpdateeDescribe.1.0({_o_0_})'

    _EXTENT_BYTES_ = 400

    # The big, scary blog of opaque data below contains a serialized PyDSDL object with the metadata of the
    # DSDL type this class is generated from. It is needed for reflection and runtime introspection.
    # Eventually we should replace this with ad-hoc constants such that no blob is needed and the generated code
    # is not dependent on PyDSDL.
    _MODEL_: _pydsdl_.DelimitedType = _restore_constant_(
        'ABzY8UsYRZ0{`V#OKclO7`AEJI8NHSucT0^4dRv3;<hLf2!tqYsX)dd>LWr(FrD>I^RKhsU1n#~xDrxw08ud#kvdcy%YmMI?2!uy'
        '35f$7K!C&>$^p~^2atN;AJ6)c_1aA;dcud@@%)eZ|8M@+E)Kl=X(7}2$vr{c4sEwcLdG5HI_Ih7vLdPYwZIP@!Ia#1jJZw?m-eZ8'
        'z|=*xcS*gjCgcX<cpl>lGIHZd!J~?ZI9tiGo_Mr|D)!lq=Z7@nVbSt^5eiO&Vied^FjhQm-p5#2;f{seIWo_9==h#iJvbAuFRD_u'
        'lzCGvDLG825Xc(|7OGe9oE}cJxRhLf++nWWwk%KZP)dY0SMKT|&N;0sIW|La7k6G@-mHLU^zKSGHxlNu8uLV~uZKAS*DyNs$g%CT'
        '7do>Zv!C|_n`L~ZzF}fHg1C+2R4IqXR%j?l&9|9?J#uuqsYr_zk0PzCNr+J94Ru1v?IaY`vnjVb6}=T%atJjAwBm@mDm^N<#w2o9'
        'gJX<`dyNrre<=qKz(b|1RtXPl=>cN7p^^_a-D@jHPet;-k|RePVL&^He`ELLI^qXPj`va$)@zpUIu!(|%xXS@C-C}T3>c@v=bhnK'
        '0SB)X9+x|g`d$TN$V+9_PHRriJRK~^zj-QhUCQfSoEykIbt9(UQ4^)ya%jO5F&Iyw)Yu8?&9W0LV8ZET(j2ML1%ws_o^5Qxb4^_h'
        '{A!Mem!ucSR$k7^L4B){OSOC{t9diarJQ+3c=2o@pk5>Xry`4?MW^suPM6=y!c3Y3yo_x6EXd0NlY0Zi_7TjC!p$AAJ3C=lyxN_('
        '@4bzihJEP5{sifuAsvEA<Jn^!=saz<j%a+7j(4z?<JEKzgwVSTdbixqyS0HZbk=d1sX{>D%-S%xbZ0TR)DMHJ1FOQ|+Q4ct2q@No'
        'E)3f49d3J`mm^k$8AH6NX6XvQX5@C?S>$&2L9TRTHNchrzBO)n1A=hUf@d&Z<>h_-np?Ab!3b<>_Al6?9ZkD~T<0CEuhFW{`(0Ur'
        '9nEO6Gr6s!M-r=UQb3D+4xho7@I{1O#%2L$x^UX+g4~2%V1d~r@VOw4t)}D_68aIZFw%}6IjCQ*V_{XD#`$1jTJK)O+Lt|mr3QRm'
        'aR$!=R?1o3$<O(G&hvEx&QEh+_!ZxsnZ!C9(fJDXl-yE@LgCli%^2IuPzz67w-80OF{VmxjZrNgDjqDxs)TNQ+X&DQE#<lwWAmMN'
        'DokC_kjO#<0W;TlNH&ARt<P%BA54xlFd)Jw961~MLTsiO9N$gA_wWP!2tUEk@C*D3ziD&QgB{z?G_~>E*{%kH)byli@7`>!lJSpY'
        'Q(*MspcFO3Jg39F8Qozcy1OJzQWTK8(9K;P&So;DT-1jpPM1RHpB3j2`}X*j4Bfls_TxIpdQDQj3re9Em|Ocb<h#h6LeU<@ViDb0'
        'Y`Bw6ac3y<oYx|zQ=!X`Pqe#$^Hqww48GD#ufixS0)@-41Rucr@DW_8?m#Hv<LZ8VRY&#XfPOrJj|jfL0N+f(w<)0n*Wq`0Phv_='
        'hCcj(VJ>&|=E^Jhb9E8FIXRFs_-joOe)Be>`TxxMq{*>X(#C_0N!Gbmc9svWYi)N*BuMbKk=Tjb*9TK25~LN`aky0wUP7*kT&jWu'
        '39cH6Gq^oIl#no`BSC_Hj6|SqA4)wVNg79z5xSwrqx54Er1+R}CX4$YriygYO!~txcI<-b)T5ZjOPIn}Fn33r8GA?4ym|<kTIpP!'
        ')QbN5ti_KGwt!ztLX{Us<S32-=KG!#VARG}@<VX^Hs`nT9`l<X@cWFuizD4V#@zfkQZJ9Bcc9|Gme3BO)(`*y'
    )
    assert isinstance(_MODEL_, _pydsdl_.DelimitedType)
