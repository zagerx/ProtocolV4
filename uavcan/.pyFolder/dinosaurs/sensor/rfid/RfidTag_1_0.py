# AUTOGENERATED, DO NOT EDIT.
#
# Source file:
# /home/zhangge/worknote/ProtocolV4/uavcan/custom_data_types/dinosaurs/sensor/rfid/RfidTag.1.0.uavcan
#
# Generated at:  2025-06-25 01:48:14.706996 UTC
# Is deprecated: no
# Fixed port ID: None
# Full name:     dinosaurs.sensor.rfid.RfidTag
# Version:       1.0
#
# pylint: skip-file
# mypy: warn_unused_ignores=False

from __future__ import annotations
from nunavut_support import Serializer as _Serializer_, Deserializer as _Deserializer_, API_VERSION as _NSAPIV_
import numpy as _np_
from numpy.typing import NDArray as _NDArray_
import pydsdl as _pydsdl_

if _NSAPIV_[0] != 1:
    raise RuntimeError(
        f"Incompatible Nunavut support API version: support { _NSAPIV_ }, package (1, 0, 0)"
    )

def _restore_constant_(encoded_string: str) -> object:
    import pickle, gzip, base64
    return pickle.loads(gzip.decompress(base64.b85decode(encoded_string)))

# noinspection PyUnresolvedReferences, PyPep8, PyPep8Naming, SpellCheckingInspection, DuplicatedCode
class RfidTag_1_0:
    """
    Generated property settings use relaxed type signatures, accepting a large variety of
    possible representations of the value, which are automatically converted to a well-defined
    internal representation. When accessing a property, this strict well-defined internal
    representation is always returned. The implicit strictification enables more precise static
    type analysis.

    The value returned by the __repr__() method may be invariant to some of the field values,
    and its format is not guaranteed to be stable. Therefore, the returned string representation
    can be used only for displaying purposes; any kind of automation build on top of that will
    be fragile and prone to mismaintenance.
    """
    def __init__(self,
                 rssi: None | int | _np_.int8 = None,
                 epc:  None | _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str = None) -> None:
        """
        dinosaurs.sensor.rfid.RfidTag.1.0
        Raises ValueError if any of the primitive values are outside the permitted range, regardless of the cast mode.
        :param rssi: saturated int8 rssi
        :param epc:  saturated uint8[<=64] epc
        """
        self._rssi: int
        self._epc:  _NDArray_[_np_.uint8]

        self.rssi = rssi if rssi is not None else 0  # type: ignore

        if epc is None:
            self.epc = _np_.array([], _np_.uint8)
        else:
            epc = epc.encode() if isinstance(epc, str) else epc  # Implicit string encoding
            if isinstance(epc, (bytes, bytearray)) and len(epc) <= 64:
                # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
                # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
                self._epc = _np_.frombuffer(epc, _np_.uint8)  # type: ignore
            elif isinstance(epc, _np_.ndarray) and epc.dtype == _np_.uint8 and epc.ndim == 1 and epc.size <= 64:  # type: ignore
                # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
                self._epc = epc
            else:
                # Last resort, slow construction of a new array. New memory may be allocated.
                epc = _np_.array(epc, _np_.uint8).flatten()
                if not epc.size <= 64:  # Length cannot be checked before casting and flattening
                    raise ValueError(f'epc: invalid array length: not {epc.size} <= 64')
                self._epc = epc
            assert isinstance(self._epc, _np_.ndarray)
            assert self._epc.dtype == _np_.uint8  # type: ignore
            assert self._epc.ndim == 1
            assert len(self._epc) <= 64

    @property
    def rssi(self) -> int:
        """
        saturated int8 rssi
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._rssi

    @rssi.setter
    def rssi(self, x: int | _np_.int8) -> None:
        """Raises ValueError if the value is outside of the permitted range, regardless of the cast mode."""
        x = int(x)
        if -128 <= x <= 127:
            self._rssi = x
        else:
            raise ValueError(f'rssi: value {x} is not in [-128, 127]')

    @property
    def epc(self) -> _NDArray_[_np_.uint8]:
        """
        saturated uint8[<=64] epc
        DSDL does not support strings natively yet. To interpret this array as a string,
        use tobytes() to convert the NumPy array to bytes, and then decode() to convert bytes to string:
        .epc.tobytes().decode()
        When assigning a string to this property, no manual conversion is necessary (it will happen automatically).
        The setter raises ValueError if the supplied value exceeds the valid range or otherwise inapplicable.
        """
        return self._epc

    @epc.setter
    def epc(self, x: _NDArray_[_np_.uint8] | list[int] | memoryview | bytes | bytearray | str) -> None:
        x = x.encode() if isinstance(x, str) else x  # Implicit string encoding
        if isinstance(x, (bytes, bytearray)) and len(x) <= 64:
            # Fast zero-copy initialization from buffer. Necessary when dealing with images, point clouds, etc.
            # Mutability will be inherited; e.g., bytes - immutable, bytearray - mutable.
            self._epc = _np_.frombuffer(x, _np_.uint8)  # type: ignore
        elif isinstance(x, _np_.ndarray) and x.dtype == _np_.uint8 and x.ndim == 1 and x.size <= 64:  # type: ignore
            # Fast binding if the source array has the same type and dimensionality. Beware of the shared reference.
            self._epc = x
        else:
            # Last resort, slow construction of a new array. New memory may be allocated.
            x = _np_.array(x, _np_.uint8).flatten()
            if not x.size <= 64:  # Length cannot be checked before casting and flattening
                raise ValueError(f'epc: invalid array length: not {x.size} <= 64')
            self._epc = x
        assert isinstance(self._epc, _np_.ndarray)
        assert self._epc.dtype == _np_.uint8  # type: ignore
        assert self._epc.ndim == 1
        assert len(self._epc) <= 64

    # noinspection PyProtectedMember
    def _serialize_(self, _ser_: _Serializer_) -> None:
        assert _ser_.current_bit_length % 8 == 0, 'Serializer is not aligned'
        _base_offset_ = _ser_.current_bit_length
        _ser_.add_aligned_i8(max(min(self.rssi, 127), -128))
        # Variable-length array: length field byte-aligned: True; all elements byte-aligned: True.
        assert len(self.epc) <= 64, 'self.epc: saturated uint8[<=64]'
        _ser_.add_aligned_u8(len(self.epc))
        _ser_.add_aligned_array_of_standard_bit_length_primitives(self.epc)
        _ser_.pad_to_alignment(8)
        assert 16 <= (_ser_.current_bit_length - _base_offset_) <= 528, \
            'Bad serialization of dinosaurs.sensor.rfid.RfidTag.1.0'

    # noinspection PyProtectedMember
    @staticmethod
    def _deserialize_(_des_: _Deserializer_) -> RfidTag_1_0:
        assert _des_.consumed_bit_length % 8 == 0, 'Deserializer is not aligned'
        _base_offset_ = _des_.consumed_bit_length
        # Temporary _f0_ holds the value of "rssi"
        _f0_ = _des_.fetch_aligned_i8()
        # Temporary _f1_ holds the value of "epc"
        # Length field byte-aligned: True; all elements byte-aligned: True.
        _len0_ = _des_.fetch_aligned_u8()
        assert _len0_ >= 0
        if _len0_ > 64:
            raise _des_.FormatError(f'Variable array length prefix {_len0_} > 64')
        _f1_ = _des_.fetch_aligned_array_of_standard_bit_length_primitives(_np_.uint8, _len0_)
        assert len(_f1_) <= 64, 'saturated uint8[<=64]'
        self = RfidTag_1_0(
            rssi=_f0_,
            epc=_f1_)
        _des_.pad_to_alignment(8)
        assert 16 <= (_des_.consumed_bit_length - _base_offset_) <= 528, \
            'Bad deserialization of dinosaurs.sensor.rfid.RfidTag.1.0'
        assert isinstance(self, RfidTag_1_0)
        return self

    def __repr__(self) -> str:
        _o_0_ = ', '.join([
            'rssi=%s' % self.rssi,
            'epc=%s' % repr(bytes(self.epc))[1:],
        ])
        return f'dinosaurs.sensor.rfid.RfidTag.1.0({_o_0_})'

    _EXTENT_BYTES_ = 66

    # The big, scary blog of opaque data below contains a serialized PyDSDL object with the metadata of the
    # DSDL type this class is generated from. It is needed for reflection and runtime introspection.
    # Eventually we should replace this with ad-hoc constants such that no blob is needed and the generated code
    # is not dependent on PyDSDL.
    _MODEL_: _pydsdl_.StructureType = _restore_constant_(
        'ABzY8UR7IY0{?wf|7#RS6u-nIcYdphXAP~kekqg`%2uHTLDAZPAZyYi(a;L*FuVJ_H_h(Mc4qd>34;EhHZagYM*m*l?B3;!<~+FD'
        '+4pVU=X0O;Ud+9Hw9u+Q?ov6-OqNH?fEGL#&v=qU#8Oq1$_NYIo!+#%N^Pa#;jje1?|*pdpZgUz&m=F%|4k;OGQ83zG9Zo8k$xhw'
        'XoG$p^1gQ+HUMpek{(;Gy)`=T`*<d9z4kA?JIT1US|pVP<Nu<{Q+hgx@^1c~fIOS@a)M>ta%Mw=)6z>V3Sq^7B9=BqUqW_YTIhp5'
        '4C^FIgk?F%zC|B*sC3GWWrfPX<2iS}H!c&>qE+&(1sCCIz49C0onyvwnQ@(cSm?hh!Fe~QjS=3>u}r1jwLBRYn`^P_E=)nKH6MDn'
        'yv4~%mIrmgyTN+6@zV@)C_vhV2I{r@NSdiY@vFFlU*orUiF$L#31!JsVTV5c)tw!+&7K6BNA-R@ly@<n32>Y2IhN$cyYI&cCcp`d'
        '{rKIxQ@;t@;Mk<sQAh3=Q>Ay8rxh{7B2l>@;<{^4DB{PGkbW;gbF1`(hXt0RAaUR=_vNaRDYqbx3=N<|qy`yeNAYLYt32m=_(|jf'
        '8}PgW|G!^}J9{R<+aogWQr7YYmwniP60Dfk(l#sj4pB=9_iH0Q7}u)#D}*Egg#(4HwPsyAD6YzR$d6}2Y-=y>G|!4e2|Fd1^(3uV'
        '2~bCW(BJasT4xp?AC-ZBQ&_l*RCo-^)Z-PrN{O!f!SJzU*vxc5b{Us2!S5;>hzv>%fyd16x-Uo+JCLzbY0E_B*WD5`s?sTtBL`M;'
        'lDlh%M=lZ~xkr0srx&dIM8(Wcq<>UlakQTy5!a_v=c&@$QiaJr(bLMRROMSg6YUirq+HU#rj@a(m`pjT?fAjop}=PIQ0cIHygNJE'
        'KGF5)S~MzR!*q5Tazj1pFefx<1CfH?4P>~*8hf+(B)OYi^toHw;~fWXxyH?`Vp=I%S28qDG&cXN;5O!1phSfw{);*^g1_L6Mh7<!'
        'Y3WRlXLMu#6Ah7f61Uo<-V3UoSl=9+K9(5m;IgSpTeli%g}x+}u|iC(5WLe!X_2I~ID3TuUEHZY7Z4;Cy$3+(G*@5-000'
    )
    assert isinstance(_MODEL_, _pydsdl_.StructureType)
